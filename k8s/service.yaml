apiVersion: v1
kind: Service
metadata:
  name: presidio-dashboard-service
  namespace: default
spec:
  selector:
    app: presidio-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
  type: LoadBalancer
